# ── CPU-only PyTorch (CRITICAL for Render 512MiB plan) ───────────────────────
# Use PyTorch's CPU wheel index BEFORE the default PyPI index so that
# sentence-transformers / transformers do not pull nvidia-cublas, nvidia-cudnn,
# triton, cuda-toolkit, etc.  This single line is what fixes the OOM on Render.
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Core web framework ────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic-settings==2.9.1
python-dotenv==1.1.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.28.1

# ── Parsing (pure C extensions, no CUDA) ──────────────────────────────────────
tree-sitter==0.23.2
tree-sitter-python==0.23.6
tree-sitter-javascript==0.23.1
tree-sitter-typescript==0.23.2

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest==8.4.2

# ── RAG / embeddings ──────────────────────────────────────────────────────────
# sentence-transformers will install torch/transformers/tokenizers via the
# CPU index URL above.  Keeping the same model (`all-MiniLM-L6-v2`, 384-d)
# preserves compatibility with all vectors already stored in Qdrant.
sentence-transformers>=3.0.0,<4.0.0
qdrant-client>=1.11.0,<2.0.0
google-genai>=1.0.0
tiktoken>=0.7.0