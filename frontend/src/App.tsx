import Dashboard from './pages/Dashboard';

/**
 * Root App component.
 *
 * For Phase 1 this just renders the Dashboard directly. In later phases
 * we'll add React Router for multi-page navigation (repos, queries, etc.).
 */
export default function App() {
  return <Dashboard />;
}
